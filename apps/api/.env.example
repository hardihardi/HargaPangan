# Contoh konfigurasi environment untuk API utama

NODE_ENV=development
PORT=4000

# Koneksi database PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pangan_db?schema=public

# Secret untuk JWT (ganti di lingkungan produksi)
JWT_SECRET=super-secret-access-key-yang-panjang
JWT_REFRESH_SECRET=super-secret-refresh-key-yang-panjang

# Origin yang diizinkan untuk CORS (pisahkan dengan koma jika banyak)
CORS_ORIGIN=http://localhost:3000

# URL service Machine Learning (FastAPI)
ML_SERVICE_URL=http://localhost:5000

# (Opsional) API data harga pangan pemerintah Indonesia
# Misalnya bila Anda mendapatkan akses API resmi dari Badan Pangan Nasional / Kementerian terkait.
# Contoh:
# GOV_API_BASE_URL=https://api.pangan.go.id
# GOV_API_KEY=isi-api-key-anda-di-sini
GOV_API_BASE_URL=
GOV_API_KEY=